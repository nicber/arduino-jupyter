.venv/
.DS_Store
__pycache__/
*.pyc
build/
.vscode/

# Generado por calib.escribir_header(): la calibración es una propiedad del
# banco, no del programa, y cada banco tiene la suya.
ControlDemo/Calibracion.h

# La calibración medida en un banco concreto. Es un dato de ese banco, no del
# proyecto: cada uno guarda la suya.
notebooks/calibracion*.json

# Y el cableado de ese banco, por lo mismo: qué puente tiene y de qué lado miran
# el imán y el sensor de corriente. Lo escribe bringup().
notebooks/cableado*.json
